// how to import the packages(basicallly the code someone have written so you dont have to write again);
import 'dart:io';

// everything is exe inside the main function
String name='tshewang';

/** data  types in the dart
 * int, 
 * double,
 * long, 
 * string,
 * boolean,
 * and the modified data types=> List(this is just the arrays), set(only collects the quque values), map **/
 // it is the best practice to write the code in the oop concepts;
 // class,objects, methods, constructors
 // the methods of writing the good oop concepts is using the
 // inheritance, abstractions, polymorphism, and the encapsulations;
main(){
  print(name);
}